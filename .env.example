# Copy this file to .env and fill your values
TMDB_API_KEY=YOUR_TMDB_KEY
PORT=8080

# Dev-only: return reset token from /api/auth/forgot-password so UI can auto-fill it
# Never enable this in production.
RETURN_RESET_TOKEN=true
